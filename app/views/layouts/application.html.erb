<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title><%= content_for(:title) || "Omni" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Omni">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%# PB-018 — cores dos status configuráveis via <style nonce> (CSP-safe; sem style inline). %>
    <%= configurable_status_styles_tag if user_signed_in? %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="app-shell">
      <% if user_signed_in? %>
        <aside class="sidebar">
          <div class="sidebar__brand">
            <span class="brand-mark">O</span>
            <span>Omni<span class="brand-sub">Continuity</span></span>
          </div>
          <%# PB-013 — busca global (GET /search) na sidebar (shell ADR-026). Funciona sem JS. %>
          <%= form_with url: search_path, method: :get, class: "sidebar__search", data: { turbo: false } do |f| %>
            <span class="search-icon" aria-hidden="true">⌕</span>
            <%= f.search_field :q, value: params[:q], placeholder: "Buscar…",
                  "aria-label": "Buscar no Omni", autocomplete: "off" %>
          <% end %>
          <%= render(SidebarComponent.new(current_path: request.path)) %>
          <div class="sidebar__footer">
            <span class="sidebar__user"><%= current_user.username %></span>
            <%= button_to "Sair", destroy_user_session_path, method: :delete, form_class: "sidebar__logout" %>
          </div>
        </aside>
      <% end %>

      <div class="app-body">
        <% if user_signed_in? %>
          <header class="topbar">
            <%# Título/subtítulo da tela (ADR-026); adoção por tela nas fatias PB-023b/c. %>
            <% if content_for?(:topbar) %>
              <%= yield :topbar %>
            <% end %>
            <div class="topbar__spacer"></div>
            <%= render "shared/running_timers_banner" %>
          </header>
        <% end %>

        <main class="app-main">
          <% flash.each do |type, message| %>
            <div class="flash flash--<%= type %>"><%= message %></div>
          <% end %>
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>
