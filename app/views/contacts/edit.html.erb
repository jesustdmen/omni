<% content_for :title, "Editar contato" %>

<nav class="breadcrumb">
  <%= link_to "Clientes", clients_path %>
  <span class="sep">/</span>
  <%= link_to @client.name, @client %>
  <span class="sep">/</span>
  <span>Editar contato</span>
</nav>

<div class="page-head"><div class="page-head__titles"><h1>Editar contato — <%= @client.name %></h1></div></div>

<div class="card form-card"><%= render "form", client: @client, contact: @contact %></div>

<%= link_to "Voltar", @client, class: "back-link" %>
