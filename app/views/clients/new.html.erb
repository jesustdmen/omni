<% content_for :title, "Novo cliente" %>

<div class="page-head"><div class="page-head__titles"><h1>Novo cliente</h1></div></div>

<div class="card form-card"><%= render "form", client: @client %></div>

<%= link_to "Voltar", (@return_to.presence || clients_path), class: "back-link" %>
