<% content_for :title, "Editar cliente" %>

<div class="page-head"><div class="page-head__titles"><h1>Editar cliente</h1></div></div>

<div class="card form-card"><%= render "form", client: @client %></div>

<%= link_to "Voltar", (@return_to.presence || @client), class: "back-link" %>
