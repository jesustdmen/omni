# Omni — Status Consolidado do Projeto

> Snapshot: **2026-06-24**. Atualizar ao fim de cada sessão de trabalho e em toda decisão/entrega.

## Status geral
- **Última fase macro concluída: Fase 5** (MVP interno, 2026-06-19). **Trilha ativa: Produto Operacional.** Loop interno completo e validado (7 smokes): listar/filtrar conversas por vínculo (F5.4), abrir conversa e renderizar turnos com segurança (F5.1) — **PII redigida** (F5.1.5) + **markdown sanitizado** (F5.2) + `tool_input` em `<pre>`; **criar tarefa a partir da conversa** (F5.3), vincular conversa↔tarefa e ver o vínculo dos dois lados (F4), navegar a task por âncoras (F5.5). **Pendências restantes são roadmap/v1** (não bloqueiam uso interno). Fases anteriores: **M1** (Fundação) ✅; **M2** (domínio CRUD; migração de domínio = N/A — RepoA inativo) ✅; **M3 MVP de metadados** (sync real 1635 conversas + folders + índice/loader lazy); **M4 MVP** (vínculo manual).
- **Trilha ativa:** **Produto Operacional** (ver `PRODUCT_BACKLOG.md`). **PB-003 concluída** (a/b/c, `0f2bc9c`); **PB-015 entregue** (sincronização operacional de conversas — importação via UI lê `output/normalized/`, **não** dispara o pipeline; ADR-011); **PB-004 concluída** (a/b/c), **PB-005** (`/demands`), **PB-006** (`/clients` + CNPJ — ADR-022) e **PB-007** (`/projects` + duplicação) entregues — **as 4 listas operacionais (tarefas/demandas/clientes/projetos) estão completas** (lacuna operacional da PB-001 fechada). **PB-013 CONCLUÍDA** — PB-013a (busca global na topbar) + PB-013b (preservação de contexto/navegação via concern central `return_to`, anti open-redirect; breadcrumbs acessíveis). **PB-014 ENTREGUE** — código legível de tarefa `TSK-000001` (sequence do Postgres; exibido em lista/detalhe/busca/links/selects; reabre o ADR-016 via addendum). **PB-016 CONCLUÍDA** — PB-016a (Omni dispara o pipeline via **agente no host**; Rails/worker não roda Python; "Sincronizar agora" faz coleta+importação) + PB-016b (agendamento interno configurável em **`/settings`**; SolidQueue `recurring.yml`; sem Tarefa do Windows); addendum ao **ADR-011**. **PB-017 — Auth/Admin seguro (single-user): ENTREGUE/publicada (2026-06-24, `477829d`)** — sem cadastro público (`:registerable` removido + `skip: :registrations`), `:recoverable` mantido com token de reset 30min + `paranoid`, `password_length` 10..128, rate limit de login por **IP e por conta/e-mail**, `mailer_sender` por ENV (SMTP separado), cookies `SameSite=Lax` explícito (HttpOnly/Secure por default+`force_ssl`), seed admin opt-in preservado; addendum ao **ADR-003**. **PB-018 — Status configurável (Tarefas/Projetos) + termos PT-BR: IMPLEMENTADO E VALIDADO (2026-06-24), aguardando aceite do PO** — tabela `configurable_statuses` + **FK composta** com garantia no banco (ON DELETE RESTRICT; CHECKs fixos removidos; ADR-024); CRUD em Configurações (criar/editar/inativar/excluir-se-não-usado; key imutável); selects de novos só ativos (+ atual); filtros/badges/show/dashboard/busca usam rótulo+cor configurados; **Demanda fixa** (Pendente/Convertida); `final` só visual. **PB-018 ENTREGUE/publicada (`ad1a10e`).** **Frente comercial — Etapa 0 registrada (2026-06-24, docs-only; ADR-025):** contrato aprovado para **Empresa Prestadora + Contratos + Cálculo + Fechamentos + Relatórios/PDF** (PB-019a Pronto p/ execução; PB-019b/020/021/022 Aprovadas; ver `PRODUCT_BACKLOG §6.1`). Princípios: contrato = prestadora+cliente (projeto opcional, prioritário); apontar **não** exige contrato (horas sem valor); **snapshot no fechamento**; sobreposição validada em Rails (EXCLUDE/btree_gist futuro). **Nada implementado.** Não iniciados: Fechamentos, Relatórios/PDF, Desktop. **F7.2 pausada** até o gate de produto.
- **Status geral:** Domínio CRUD completo (F2). **Conversas (metadados) com sync real**: 1635 conversas de `summaries.jsonl` (idempotente; `source_nil=0`, `workspace_hash_nil=13`, `title_nil=1067`). **F3.3** resolveu folders (`WorkspaceMap=86`, órfãos 86→3; usuário redigido `<USER>`). **Pré-F5**: índice de turnos (offsets) + loader lazy — 129.482 refs, covered 1635/1635, sem conteúdo no banco. **F5 MVP**: render read-only seguro + PII + markdown sanitizado + lista acionável + criar/vincular tarefa + navegação por âncoras. **PB-003 (a+b+c)**: controle de tempo — **concluída**. **PB-015**: sincronização operacional de conversas (importação via UI em background, lê `output/normalized/` com allowlist, não dispara pipeline; advisory lock + settle/verify de fingerprint; status agregado `SyncExecution`; worker `omni_jobs` isolado). **Sync validado ponta a ponta (2026-06-21):** pipeline real regenerou o output → **+12 conversas novas**. Estado atual do banco dev: **conversations=1647 · turn_sources=1 · conversation_turn_refs=134653 · sync_runs · conversation_links e tasks preservados**. **PB-004a**: lista operacional de `/tasks` (busca por título/descrição com `%`/`_` escapados, filtros status/tipo/cliente, paginação 10/25/50/100, ações ver/editar/excluir, estados vazios; sem N+1). **PB-004b**: checklist persistente da tarefa (`ChecklistItem` FK cascade; adicionar/marcar/editar in-place/excluir; contador; sem JS). **PB-004c**: vínculo 1:1 demanda↔tarefa (`tasks.demand_id` FK RESTRICT + único parcial; conversão com lock+revalidação; `DeleteTask` devolve demanda a pending; demanda vinculada não-excluível; aba Demanda na tarefa + link na demanda; reconciliação dev `4549551a↔8bcbbcb5`) — **PB-004 concluída**. **PB-005**: lista operacional de `/demands` (busca; filtros; conversão pela lista; sem N+1). **PB-006**: `/clients` Empresas/Contatos + contato principal único + busca de CNPJ na BrasilAPI **pelo navegador** (ADR-022 revertida em 2026-06-23 — proxy no Rails/`Cnpj::Lookup`/rota `cnpj_lookup` removidos; CSP `connect-src` p/ BrasilAPI). **PB-007**: `/projects` operacional (busca nome/descrição; filtros cliente/status; status fechado 4 valores com CHECK + labels PT-BR + select; **duplicação transacional** sem copiar datas/orçamento/tarefas; período/orçamento). **As 4 listas operacionais (tarefas/demandas/clientes/projetos) estão completas.** **PB-013a**: busca global (`GET /search`) agrupada por categoria (badge de tipo, "Encontrado em", card-link "Ir →" sem JS, "Voltar"); tarefas por checklist/apontamento (DISTINCT); conversas por título com source/workspace. **PB-013b**: preservação de contexto/navegação — concern central `ReturnNavigation` (`return_to` sanitizado anti open-redirect: só caminho interno, preserva query/fragmento, rejeita scheme/host/`//host`/backslash/CR-LF; fallback por recurso); listas/detalhes/forms/busca honram o contexto (busca/filtros/paginação/aba); contatos e apontamentos voltam conforme a origem; breadcrumbs com `aria-label` e sem UUID. **PB-013 CONCLUÍDA.** **PB-014**: código legível de tarefa — `tasks.code_number` (bigint) por **sequence do Postgres** (`DEFAULT nextval`; concorrência segura, nunca `maximum+1`) → `Task#code`=`TSK-000001`; backfill determinístico (`created_at,id`), `NOT NULL`+unique, `attr_readonly`/fora dos strong params; URLs seguem por UUID (sem rota por código); exclusão não reutiliza; exibido em lista/detalhe/busca/links/selects; busca por código (completo/número, case-insensitive) em OR com texto. Banco dev: 6 tarefas `TSK-000001..000006`, 0 nulos/duplicados. Reabre o **ADR-016** (addendum). **PB-016 (a+b)**: sincronização completa pelo Omni — o pipeline (RepoB, Windows-nativo) roda **no host** via **agente** (`app/script/pipeline_agent.py`, stdlib; HTTP+token; comando fixo; timeout; saída segura); `Sync::PipelineRunner` é cliente HTTP (health→run) e `RunConversationsSync` faz **coleta antes da importação** (gated por `OMNI_RUN_PIPELINE_INTERNALLY`, default off); **agente offline degrada** (importa o output atual com aviso), falha real **aborta antes de importar** (índice preservado). **Agendamento interno** em `/settings` (`SyncSchedule` singleton + `ScheduledSyncJob` recorrente, `recurring.yml`), **sem Tarefa do Windows**. Validado ponta a ponta (coleta real exit=0; +482 turn_refs; vínculos/tarefas preservados). Migrations aditivas: `sync_executions` (current_step/pipeline_exit_code/pipeline_summary) + `sync_schedules`. Correção pós-PB-006: CNPJ 429 (rate-limit) com mensagem clara. **Reversão CNPJ (2026-06-23):** consulta voltou ao navegador (proxy no Rails removido; CSP `connect-src` p/ BrasilAPI; ADR-022 revertida). **Melhoria UX (2026-06-23):** paginação amigável + "Mostrar tudo" em todas as listas e nos turnos. **Timezone (2026-06-23, ADR-023):** timezone operacional = **Brasília** (`America/Sao_Paulo`, UTC−3); banco persiste **UTC**; `datetime-local` parseado em `Time.zone`; `date` de TimeEntry deriva do dia operacional em Brasília; exibição via helpers `local_*`. Sem backfill. **PB-017 (2026-06-24, `477829d`) — Auth/Admin seguro ENTREGUE/publicada:** sem cadastro público; reset 30min + paranoid; password_length 10..128; rate limit login por IP + conta/e-mail; mailer_sender por ENV (SMTP separado); cookies SameSite=Lax explícito (HttpOnly/Secure por default+force_ssl); seed admin opt-in preservado; addendum ao ADR-003; sem credenciais no diff. **PB-018 (2026-06-24) — Status configurável (Tarefas/Projetos) + termos PT-BR (implementado/validado, aceite pendente):** tabela `configurable_statuses` (entity_type/key/name/color/position/active/final) + seed; integridade no banco via **FK composta** `(status_entity, status)→(entity_type, key)` ON DELETE RESTRICT (CHECKs fixos removidos; ADR-024); `status` segue string/key; CRUD em Configurações (criar/editar/inativar/excluir-se-não-usado; key imutável); selects de novos só ativos (+ atual); listas/badges/show/dashboard/busca usam rótulo+cor configurados; **Demanda fixa** (pending→Pendente/converted→Convertida); `final` só visual. **Cor do badge CSP-safe:** classe `cfg-status--<entity>-<key>` + bloco `<style nonce>` no layout (sem `style` inline — a CSP `style-src 'self' 'nonce-…'` bloqueava o inline; ADR-012). **Termos PT-BR também** em **Tipo de tarefa** (Suporte/Dúvida/Implementação/Desenvolvimento/Comercial), **Origem** (Telefone/E-mail/Reunião/Chat/WhatsApp/Outro) e **Prioridade** de demanda (Baixa/Média/Alta) — listas fixas (mapas no model), só exibição. Métricas correntes: **676 runs, 2607 assertions, 0 failures/errors/skips** (com `OMNI_RUN_PIPELINE_INTERNALLY=0`; com a flag ON o container tem 2 falhas **pré-existentes** de pipeline/PB-016, sem relação com status/auth); rubocop **0**; brakeman **0**; bundler-audit **0**.
- **Stack provisionada:** Rails 8.1.3 + PostgreSQL 16 via Docker (sem instalar nada no host; `_origem/` intocado).
- **Bloqueadores da Fase 2:** Nenhum, exceto autorização explícita do usuário.
- **Bloqueadores futuros (Fase 3):** **resolvidos (F3.0→F3.3)** — corpus; `thread_id → shard` via **ADR-018** (turnos fora); `schema_version` por-run; importer idempotente; sync real validado; folders resolvidos (**ADR-020**). Item opcional: limpar `sync_runs/sync_run_items` de auditoria no dev.
- **Ação de segurança:** dump do RepoA fora do versionamento — protegido no repo de planejamento via `.gitignore`; RepoA tratado como referência/leitura.
- **Trilha ativa: Produto Operacional (replanejamento, 2026-06-19).** F7.2 **pausada** até o gate operacional (ver `PRODUCT_BACKLOG.md §5`). Entregues: **PB-001** auditoria (`3037a00`) + **PB-002** revisão da matriz + **PB-003a** (`d11f099`), **PB-003b** (`5fcf125`) e **PB-003c** (`0f2bc9c`) controle de tempo (timer start/stop + duração em segundos + paralelismo configurável + histórico operacional + agrupamento e subtotal por dia + **retroativo assistido + proteção do running + aviso/lista global de timers + nota de sobreposição**, **aceite manual do PO**). **PB-003 integralmente concluída** (entregue, aceita e publicada). **Próximas decisões do PO:** busca/filtros/paginação das listas (PB-004/005/006) · **PB-013 UX de navegação/contexto** · **PB-014 código legível de tarefa**. Governança em `PRODUCT_BACKLOG.md`/`PRODUCT_GAP_REVIEW.md`.
- **Última decisão/entrega (código):** **PB-018 — Status configurável (Tarefas/Projetos) + termos PT-BR (2026-06-24; IMPLEMENTADO E VALIDADO, aguardando aceite do PO; ADR-024)**: status de Tarefas/Projetos passam a ser **configuráveis** (nome/chave/cor/ordem/ativo/finalizador) em **Configurações** (`/settings`), com integridade no **banco** via **FK composta** `(status_entity, status) → configurable_statuses(entity_type, key)` **ON DELETE RESTRICT** (CHECKs fixos de status removidos). `tasks.status`/`projects.status` seguem **string/key** (impacto mínimo). `ConfigurableStatus` valida key snake_case única por entidade, nome, **cor hex (allowlist)**, posição, ativo, final. CRUD via `ConfigurableStatusesController` + `ConfigurableStatusPolicy` (ADR-014). Selects de novos registros só mostram **ativos** (+ o status atual do registro, mesmo inativo); **excluir status em uso é bloqueado** (mensagem + FK); filtros/forms/badges/show/dashboard/busca usam **rótulo+cor configurados** (`configurable_status_badge`). **Demanda permanece fixa** (enum pending/converted) com rótulos PT-BR (Pendente/Convertida). **`final` é só visual** (filtros/exibição; sem regra de negócio). Fixture `configurable_statuses` satisfaz a FK nos testes. **Cor do badge CSP-safe:** classe `cfg-status--<entity>-<key>` + `<style nonce>` no layout (sem `style` inline — a CSP bloqueava o inline; nonce real verificado == header). **Termos PT-BR também** em Tipo de tarefa (`Task::TYPE_LABELS`), Origem e Prioridade de demanda (`Demand::ORIGIN_LABELS`/`PRIORITY_LABELS`) — listas fixas, só exibição (eram `humanize` em inglês). Suíte **676/2607/0** (`OMNI_RUN_PIPELINE_INTERNALLY=0`; +29 testes); rubocop **0**; brakeman **0**; zeitwerk OK; `git diff --check` limpo. **Riscos residuais:** `key` imutável após criada (FK); reatribuição em massa fora de escopo; Tipo/Origem/Prioridade são PT-BR fixos (não configuráveis). **Não toca PB-017/Contratos/Fechamentos/Empresa prestadora/Conversas/Desktop.** Antes: **PB-017 — Auth/Admin seguro (2026-06-24, `477829d`; ENTREGUE/publicada em `main`; addendum ao ADR-003)**: endurecimento de autenticação single-user/somente-Admin via Devise + skill **secure-auth**. **Sem cadastro público** (`:registerable` removido do `User`; `devise_for :users, skip: [:registrations]` → somem `/users/sign_up`, `POST /users` etc.; rota inexistente → 404; `:recoverable` mantido). **Reset** com `reset_password_within=30min` + `paranoid` (anti-enumeração). **Política de senha** `password_length=10..128`. **Rate limit de login por IP e por conta/e-mail** (rack-attack; e-mail normalizado; ausência de e-mail não conta). **`mailer_sender` por ENV** `OMNI_MAIL_FROM` (SMTP separado em ENV/credentials). **Cookies** `SameSite=Lax` explícito (`config/initializers/session_store.rb`); HttpOnly default; Secure por `force_ssl`. **Seed admin** opt-in preservado. Senhas de fixture de teste elevadas para ≥10 chars (`secret123`→`secret12345`, 34 arquivos). **Sem credenciais no diff** (só senhas dummy de teste; `secret_key`/`pepper`/`master.key`/SMTP fora). Suíte **647/2483/0** com `OMNI_RUN_PIPELINE_INTERNALLY=0` (baseline 629/2428; +18 testes auth); rubocop **194/0**; brakeman **0**; zeitwerk OK; `git diff --check` limpo. **Risco residual:** custo bcrypt=10 (compat. ADR-003; abaixo do alvo 100–300ms — elevar é decisão do PO). **Nota de integridade:** com a flag de pipeline ON (estado do container) o baseline já tinha **2 falhas pré-existentes** (PB-016: `run_conversations_sync_test.rb:223`, `sync_executions_test.rb:152`) sem relação com auth; PB-017 não as introduz. Antes: **Timezone operacional Brasília (2026-06-23, branch `fix-timezone-brasilia`, ADR-023)**: `config.time_zone="Brasilia"` (`America/Sao_Paulo`, UTC−3) + `default_timezone=:utc` (banco em UTC). `datetime-local` parseado em `Time.zone` (retroativo "09:00" = 09:00 BR → 12:00 UTC); `TimeEntry#date` deriva do dia operacional via `in_time_zone.to_date` (`derive_date_and_duration`/`start_for`); `duration`/`stop!` inalterados. Exibição via helpers centrais `local_datetime`/`local_time`/`local_date` substituindo `strftime` nos pontos de TimeEntry (index/running/show/_form + timer/histórico em tasks/show). **Sem backfill** (registros existentes só passam a ser exibidos em Brasília; `date` segue coerente). Aceite visual do PO. Suíte **629/2428/0** (novo `timezone_brasilia_test.rb`: config, retroativo, fronteira 00:30/22:00 BR, start_for, stop!, agrupamento por dia); rubocop 192/0; brakeman 0. Antes: **Paginação amigável — melhoria UX transversal (2026-06-23, branch `pagination-friendly` rebaseada sobre `main`)**: helper único `pagination_nav` (« Primeira · ‹ Anterior · "Página X de Y · N item(s)" · Próxima › · Última »; preserva busca/filtros/per_page/aba) em **todas as listas** (tasks/demands/projects/clients/conversations) e nos **turnos** (`Conversations::TurnListComponent`). Concern `Paginated`: tamanho por allowlist (10/25/50/100) + **"Mostrar tudo"** (teto `ALL_CAP=1000` + aviso quando excede). Turnos com seletor próprio (50/100/200; `TURNS_PER_PAGE_OPTIONS`). Sem item de backlog dedicado (melhoria UX); sem migration/schema. Aceite visual do PO. Suíte **621/2398/0**; rubocop 191/0; brakeman 0. Antes: **Reversão CNPJ (docs + código, 2026-06-23, `7c2e2e4`/`96c45c8`/`7b308bd`)**: consulta de CNPJ voltou ao **navegador** (proxy no Rails/`Cnpj::Lookup`/rota removidos; CSP `connect-src` p/ BrasilAPI); **ADR-022 revertida** (addendum). Antes: **PB-016 — sincronização completa pelo Omni (2026-06-22, branch `pb-016a-internal-pipeline`)**: o botão "Sincronizar agora" passa a executar **coleta (pipeline) + importação**, e há **agendamento configurável em Configurações** — sem PowerShell/Tarefa do Windows. **PB-016a:** o pipeline (RepoB, Windows-nativo: lê `%APPDATA%`/`.codex`/`.claude`, exige `APPDATA`) **não roda no container**; roda **no host** via **agente** (`app/script/pipeline_agent.py`, stdlib; HTTP `/health` `/run`; token; comando FIXO; timeout; lock; saída segura). `Sync::PipelineRunner` vira cliente HTTP; `RunConversationsSync` faz coleta **antes** da importação (gated por `OMNI_RUN_PIPELINE_INTERNALLY`, default off = PB-015). **Resiliência:** agente offline → degrada (importa o output atual com aviso); falha real (exit≠0/timeout) **aborta antes de importar** (índice preservado). Auto-start/restart do agente (`.devstack/agent.sh`+`up.sh`). **PB-016b:** `SyncSchedule` (singleton: ligar/desligar + intervalo 15min…24h) + `ScheduledSyncJob` recorrente (SolidQueue `recurring.yml`, tick/min) disparam o mesmo fluxo quando vencido e sem execução ativa; mora em **`/settings`** (`SettingsController`; antes placeholder); `/sync_runs` fica só com o manual + atalho. Migrations aditivas (`sync_executions`: current_step/pipeline_exit_code/pipeline_summary; `sync_schedules`). **Addendum ao ADR-011.** Aceite do PO + validação real ponta a ponta (coleta exit=0; +482 turn_refs; vínculos/tarefas preservados). Inclui correção pós-PB-006 (CNPJ 429 → mensagem clara). Suíte **614/2363/0**; rubocop 190/0; brakeman 0. Antes: **PB-014 — código legível de tarefa (2026-06-22, branch `pb-014-readable-task-code`)**: `tasks.code_number` (bigint) por **sequence do PostgreSQL** (`tasks_code_number_seq`, `DEFAULT nextval`, `OWNED BY` a coluna — concorrência segura, **nunca `maximum+1`**) → `Task#code` = **`TSK-%06d`**. Migration reversível: nullable → sequence → **backfill determinístico** (`created_at ASC, id ASC`) → `DEFAULT nextval` → `NOT NULL` → índice **unique** (schema dumpa `bigserial`). **Read-only** (`attr_readonly` + fora dos strong params; params ignorado). **URLs seguem por UUID** (sem rota/lookup por código); **exclusão não reutiliza**. Exibido em lista/detalhe de tarefas, busca global ("Encontrado em: Código"), links em demandas/conversas/apontamentos/dashboard e selects (`TSK-000001 — Título`); **busca por código** (completo/número, case-insensitive) em OR com texto (`%/_` escapados). Banco dev: 6 tarefas `TSK-000001..000006`, 0 nulos/duplicados. **Addendum ao ADR-016.** Aceite do PO. Suíte **577/2220/0** (11 unit incl. concorrência + 15 integração); rubocop 179/0; brakeman 0. Antes: **Correção pós-PB-013 — loader de turnos (2026-06-22, `e4663ff`)**: `ConversationTurns::LazyLoader#partial_hash` alinhado ao builder (3 janelas: cabeça+miolo+cauda); a divergência (2 janelas) marcava todo índice válido como `:stale` e ocultava o conteúdo de toda conversa. Sem migration/dados/pipeline. Suíte 551/2145/0. Antes: **PB-013b — preservação de contexto/navegação (2026-06-22, branch `pb-013b-navigation-context`)**: **mecanismo central `return_to`** via concern **`ReturnNavigation`** (sanitizador único: aceita só caminho interno absoluto, preserva query/fragmento, **rejeita** scheme/host, `//host`, backslash, CR/LF/controle e tamanho >2000 — **anti open-redirect**; **fallback por recurso** `safe_return_to`; referer interno só como fallback secundário). Listas (tarefas/demandas/projetos/clientes-empresas/contatos/conversas) levam `return_to=request.fullpath` (busca/filtros/paginação/per_page/**aba**) em Ver/Editar/Excluir; detalhes (Voltar/breadcrumb/Editar/Excluir), forms (hidden + Cancelar) e pages new/edit honram o contexto; **busca global** abre resultado e volta aos mesmos resultados (PB-013a intacta); **contatos** voltam conforme origem (cliente vs `/clients?tab=contacts`); **apontamentos** voltam à tarefa (`#tab-time`) ou à lista global conforme origem; **breadcrumbs** com `aria-label="Breadcrumb"`, sem UUID; sem reformulação visual; sem migration/schema/dependência. Aceite do PO (busca a validar em uso). Suíte **547/2135/0** (20 unit do sanitizador + 25 de fluxo); rubocop 174/0; brakeman 0. **PB-013 integralmente concluída.** Antes: **PB-013a — busca global (2026-06-21, branch `pb-013-global-search`)**: `GET /search` (read-only; auth Devise) com `GlobalSearch` em **6 categorias** (tarefas/demandas/projetos/clientes/contatos/conversas), top-5 + "ver todos"; tarefas por título/descrição/**checklist/apontamento** com **DISTINCT** + "Encontrado em"; conversas por título com **source/workspace** (sem turnos — ADR-021); cada resultado é **card-link único** com **"Ir →"** (sem JS, a11y, responsivo) + **"← Voltar"** (referer interno; fallback Dashboard); topbar virou form GET funcional. **PB-013 segue parcial** (PB-013b: breadcrumbs + preservação de filtros/contexto — pendente). Aceite do PO. Suíte **502/2005/0**; rubocop 171/0; brakeman 0. Antes: **PB-007 — projetos operacionais + duplicação (2026-06-21, branch `pb-007-projects-operational`)**: `/projects` com busca nome/descrição (`%`/`_` escapados) + filtros cliente/status + paginação (ordem `name asc, id asc`); **status fechado** (planning/in_progress/completed/on_hold) com **CHECK no banco** + labels PT-BR + **select no form**; **`DuplicateProject`** transacional (copia só cliente/descrição, status→planning, sem orçamento/datas/tarefas/vínculos, leva à edição, rollback em falha); colunas período/orçamento; `end_date`=prazo/fim, término≥início, orçamento informativo; `policy_scope`; `includes(:client)` sem N+1; **migration aditiva** (CHECK). Aceite do PO. Suíte **483/1924/0**; rubocop 167/0; brakeman 0. Antes: **PB-006 — clientes/contatos operacionais + busca de CNPJ (2026-06-21, branch `pb-006-clients-contacts`)**: `/clients` com abas **Empresas/Contatos** (server-side `tab=`); Empresas com busca nome/fantasia/**CNPJ com-ou-sem pontuação** + filtro status + paginação (ordem `name asc, id asc`) + contato principal na coluna; Contatos com busca nome/e-mail/telefone/cargo + filtros cliente/status/principal; **≤1 contato principal/cliente** (índice único parcial + regra transacional que desmarca o anterior). **Cadastro via busca de CNPJ por proxy no Rails** (`GET /clients/cnpj_lookup` → `Cnpj::Lookup` na BrasilAPI; host fixo allowlist + timeout + falha graciosa, sem persistir resposta crua; autopreenche o form via Stimulus) — **ADR-022**. *[Supersedido em 2026-06-23: o proxy foi removido e a consulta voltou ao navegador — ver "Status geral" acima e ADR-022 revertida.]* `policy_scope`; `includes` sem N+1; **migration aditiva** (índice de contato principal). Aceite do PO. Suíte **459/1813/0**; rubocop 163/0; brakeman 0. Antes: **PB-005 — lista operacional de `/demands` (2026-06-21, branch `pb-005-demands-operational`)**: busca por título/descrição/observações (`ILIKE`; `%`/`_` escapados), filtros prioridade/origem/status/cliente (allowlist; inválidos ignorados), paginação `page`/`per_page` (10/25/50/100, default 50; total antes de limit/offset; ordem estável `created_at desc, id desc`; links preservam params; página inválida → 1), tabela com trecho da descrição + ações; **conversão pela lista por estado** (pending+cliente → Converter; sem cliente → "sem cliente"; converted → Abrir tarefa) reutilizando `ConvertDemand`/vínculo PB-004c; estados vazios; `includes(:client, :converted_task)` sem N+1; **sem migration/dependência**. Aceite do PO. Suíte **426/1691/0**; rubocop 157/0; brakeman 0. Antes: **PB-004c — vínculo demanda↔tarefa (2026-06-21, branch `pb-004c-demand-task-link`)**: `tasks.demand_id` (uuid null, FK **ON DELETE RESTRICT**, índice único parcial); `Task belongs_to :origin_demand` / `Demand has_one :converted_task` (restrict_with_error). `ConvertDemand` com **lock + revalidação pós-lock** cria a tarefa já vinculada (concorrência não gera 2ª); serviço explícito **`DeleteTask`** devolve a demanda a **pending**/limpa `converted_at` ao excluir a tarefa (rollback em falha); demanda vinculada **não-excluível** (bloqueio + FK RESTRICT). UI: aba **Demanda** na tarefa + link p/ tarefa na demanda. **Reconciliação dev** transacional (vínculo histórico `4549551a↔8bcbbcb5` após validar 9 condições; pending intactas). **Migration aditiva.** Aceite do PO. Suíte **401/1601/0**; rubocop 156/0; brakeman 0. Antes: **PB-004b — checklist persistente da tarefa (2026-06-21, branch `pb-004b-task-checklist`)**: `ChecklistItem` (uuid; `task_id` FK **ON DELETE CASCADE**; `content` trim+presence; `completed` default false; ordem `created_at, id`, sem `position`/`default_scope`); rotas aninhadas + `ChecklistItemPolicy` (ADR-014); itens **sempre escopados pela tarefa da URL**; strong params só `content`/`completed`. UI na seção Detalhes: contador, estado vazio, adicionar/marcar/**editar in-place** (linha alterna exibição↔edição via `<details>`, **sem JS**)/excluir. **Migration aditiva** (`checklist_items`). Aceite do PO. Suíte **379/1503/0**; rubocop 152/0; brakeman 0. Antes: **PB-004a — lista operacional de `/tasks` (2026-06-21, branch `pb-004a-tasks-list`)**: busca por título/descrição (`ILIKE` case-insensitive; `%`/`_` escapados), filtros combináveis status/tipo/cliente (allowlist; inválidos ignorados), paginação `page`/`per_page` (10/25/50/100, default 50; total antes de limit/offset; ordenação estável `created_at desc, id desc`; links preservam params; página inválida → 1), tabela (título+trecho/cliente/projeto/status/tipo/criada em/ações), ações ver/editar/excluir (confirmação) + "Nova tarefa", estados vazios (sem tarefas / sem resultado com "Limpar filtros"); `policy_scope` + `includes` sem N+1; **sem migration/dependência**. Aceite do PO. Suíte **355/1432/0**; rubocop 146/0; brakeman 0. Antes: **PB-015 — sincronização operacional de conversas (2026-06-21, branch `pb-015-sync-operacional`)**: tela `/sync_runs` operacional com **"Atualizar conversas no Omni"** (enfileira `SyncConversationsJob`; CSRF/Pundit); `Sync::RunConversationsSync` lê **só** `/normalized` (allowlist), ordem **ImportSummaries → BuildConversationTurnRefs**, **advisory lock** + **settle/verify** de fingerprint (preserva índice anterior em falha), **status agregado `SyncExecution`**, **botão desabilitado + barra de progresso + auto-refresh**; **worker `omni_jobs`** isolado no devstack; **script externo** `SyncOmniConversations_PB015_v1.ps1` (pipeline + enfileira import, sem o Rails rodar Python); **correção do falso no-op** do fingerprint de turn_refs. **Não dispara o pipeline** (ADR-011). **Aceite manual do PO + validação ponta a ponta** (+12 conversas reais → 1647). Suíte **332/1350/0**; rubocop 145/0; brakeman 0. Migration aditiva (`sync_executions`). **PB-016** (agendador interno) proposto. Antes: **PB-003c — apontamento retroativo assistido + timers globais (2026-06-20, `0f2bc9c`)**: registro retroativo por **início+término** com **duração e `date` derivadas no model**; `duration`/`date`/`is_running` fora do form/strong params; `end_time` obrigatório p/ não running; **proteção do timer running** (end_time nil, duration 0, edição genérica só descrição); **aviso global de timers** na topbar (1 COUNT) + **lista global** `/time_entries/running` (sem N+1); **nota de sobreposição**; ajuste visual final do histórico (total geral no `tfoot`). **Aceite manual do PO.** Suíte **308/1246/0**; rubocop 135/0; brakeman 0. **PB-003 integralmente concluída.** Antes: **PB-003b — agrupamento de apontamentos por dia + subtotal diário (2026-06-20, `5fcf125`)**: `/tasks/:id` agrupa o "Histórico de Apontamentos" **por data** (grupos desc; itens por horário desc), com **subtotal diário** (exclui timers em andamento); total geral e ações inline preservados; sem N+1; **aceite manual do PO**. Suíte **295/1181/0**; rubocop 135/0; brakeman 0. Antes: **PB-003a — controle de tempo operacional (2026-06-19, `d11f099`)**: `time_entries` ganha **timer start/stop** (`TaskTimersController`/`TimeEntriesController#stop`), **`duration` calculado em segundos** (`stop!`), **`ALLOW_PARALLEL_RUNNING_TIMERS`** (default true), **índice único parcial** (1 timer aberto/tarefa) + validação, e **"Histórico de Apontamentos"** com ações inline (ver/editar/excluir/parar; ícones lucide + cores). Antes: **F7.1 — endurecimento de produção + admin seed**: `production.rb` por ENV (`force_ssl`/`assume_ssl`/`config.hosts`/mailer host; `/up` fora do redirect; default seguro, sem domínio hardcoded); `db/seeds.rb` admin **opt-in/idempotente** (`OMNI_SEED_ADMIN` + `OMNI_ADMIN_*`; sem senha padrão/sem logar senha; promove sem trocar senha; no-op sem flag → CI verde). Sem schema/Solid/Kamal/Dockerfile/credentials. Suíte **279/1087/0**; rubocop 133/0; brakeman 0; bundler-audit 0. Antes: **F5 MVP interno concluído** (F5.1→F5.5). Antes: **F5.5 — usabilidade da task: navegação por âncoras (2026-06-19)**: abas cosméticas de `tasks/show` viram **links de âncora honestos (sem JS)** (`#tab-detalhes`/`#tab-time`/`#tab-conversas`); "Conversas (N)" mostra contagem de vínculos; "Histórico"/"Demanda" permanecem "em breve" sem `href`; CSS escopado (`scroll-margin-top`, realce `:target`). **Sem abas dinâmicas JS** (seguem roadmap). Suíte **274/1068/0**; rubocop 132/0; brakeman 0; bundler-audit 0. Antes: **F5.4 — lista de conversas acionável / status de vínculo (CV-04, `9537eac`)**: `/conversations` com coluna **Vínculo** + filtro `link` (`none`/`primary`/`mention`); eager loading sem N+1. Antes: **F5.3 — criar tarefa a partir da conversa (UI-10, `c104203`)**: `ConversationTasksController` cria `Task` + `ConversationLink` `primary`/`manual` **em transação** (rollback sem órfã); ação oculta quando já há `primary`. Antes: **F5.2 — markdown sanitizado no render** (`f3075e0`): `text` vira **markdown (GFM) → HTML sanitizado** via `ConversationTurns::MarkdownRenderer` (`commonmarker 2.8.2` seguro + `SafeListSanitizer` allowlist + hardening de links); `tool_input` em `<pre>`; PII antes do markdown; `html_safe` só no renderer. Antes: **F5.1.5 — redação de PII em `text`/`tool_input` (`821f495`)**: `ConversationTurns::PiiRedactor` (conservador/idempotente) cobrindo e-mail/Bearer/`token|api_key|secret|password|access_token|refresh_token`/paths `Users|home` (Unix/Windows/`file://`) → `<EMAIL>`/`<SECRET>`/`<USER>`. Antes: **F5.1.4 — limpeza DB-only dos resíduos sintéticos** (9 refs + 3 turn_sources `/tmp` + 3 conversas `tXSS*` + 3 sync_runs `/tmp`; backup `tmp/dev_backup_pre_f514_20260618_095619.sql` gitignored). DB dev fiel ao real: **conversations=1635 · turn_sources=1 · conversation_turn_refs=129482 · sync_runs=5 · conversation_links=1 · órfãs=0**; conversa real e loader `:ok` (177) preservados; nenhum arquivo alterado. Antes: **F5.1.3** (ocultar `source_file` em `/sync_runs/:id` via `safe_basename`); **F5.1.2** (consolidação documental + persistência do runtime): registro da F5.1.1, addendum ao ADR-013 (`personal` boolean + b1), padronização "Omni" e **persistência do mount `/normalized:ro`** no `.devstack/up.sh` (sem mudar comportamento de app). Antes: **F5.1.1** (`a01efbd`, fix do artefato ERB `). %>` + cor de role por allowlist) e **F5.1** (`2cc605b`, render read-only de turnos: `LazyLoader` via `Conversations::TurnListComponent`, `TURNS_PER_PAGE=50`, `personal`=b1, auto-escape, CSP restrita; validado em conversa real de 177 turnos). Pré-F5: índice de turnos + loader (`ed27143`).
- **Próxima decisão/ação necessária:** **autorizar a execução da PB-019a — Empresa Prestadora (CRUD)**, primeira fatia da **frente comercial** (Empresa Prestadora + Contratos → Cálculo → Fechamentos → Relatórios/PDF), cujo contrato foi registrado na **Etapa 0 (docs-only, 2026-06-24; ADR-025 + PRODUCT_BACKLOG §6.1)**. **PB-017 e PB-018 ENTREGUES/publicadas** (`477829d`, `ad1a10e`). Não iniciados: Fechamentos (PB-021), Relatórios/PDF (PB-022), Desktop, Revisão de código. **F7.2 retoma só após o gate de produto (GP-01..07).** *(PB-003 concluída; PB-015 entregue/aceita; PB-004 concluída (a/b/c); PB-005/PB-006/PB-007 entregues/aceitas; **PB-013 CONCLUÍDA (a+b)**; **PB-014 entregue/aceita**; **PB-016 CONCLUÍDA (a+b), aceita**; **PB-017 entregue/publicada**; **PB-018 entregue/publicada**; **frente comercial PB-019..PB-022 = contrato registrado (Etapa 0), implementação não iniciada**; F7.1 entregue; F5 MVP interno concluído.)*

## Semáforo por área
| Área | Status | Observação |
|---|---|---|
| Arquitetura | 🟢 Verde | ADRs aceitos; baseline congelado |
| Rails Foundation | 🟢 Verde | App Rails 8.1.3 operacional; 15 testes verdes; CI local verde |
| Banco de dados | 🟢 Verde | Postgres 16 (omni_db) + migration de `users` aplicada; domínio na F2 |
| Migração Repo A | 🟢 Verde | **M2 concluído** — domínio CRUD completo (Client+Contact+Project+Task+Demand+ConvertDemand+TimeEntry); **migração de dados reais = N/A** (RepoA inativo, domínio vazio na origem) |
| Pipeline Repo B | 🟢 Verde | Externo, estável, intocado |
| Importação de conversas | 🟢 Verde (MVP metadados) | **M3 MVP de metadados CONCLUÍDO:** sync real idempotente (1635 conversas; `source_nil=0`/`workspace_hash_nil=13`/`title_nil=1067`), folders (`orphan` 86→3), índice de turnos + loader lazy (ADR-021; 129.482 refs, covered 1635/1635, sem conteúdo no banco). **Pendências → roadmap:** OP-01/OP-03, CV-03, CV-10. UI rica de turnos = F5 |
| Vínculo conversa/tarefa | 🟢 Verde (MVP manual) | **M4 MVP manual CONCLUÍDO:** `conversation_links` (≤1 primário, reversível, auditável) + counters transacionais (LK-01/02/03/07/08). **Pendências → v1:** scorer/sugestões/auto-link (LK-04/05), aceite em lote (LK-06), `time_entry_id` |
| UI | 🟡 Amarelo | **F5 MVP interno CONCLUÍDO**: `/conversations` lista acionável (F5.4) + detalhe com turnos read-only seguros (markdown F5.2, PII F5.1.5, `tool_input` em `<pre>`), criar tarefa da conversa (F5.3), navegação da task por âncoras (F5.5), **Histórico de Apontamentos com timer (PB-003a)**. Amarelo = telas avançadas pendentes (UI-01 dashboard, UI-04 aba rica, UI-09 Ctrl+L, busca/triagem, PB-013 navegação) = roadmap/v1 |
| Testes | 🟢 Verde | Suíte verde **294/1151/0** (ver métricas correntes acima); corpus sintético (`test/fixtures/normalized_corpus/`); cobertura por controller/serviço/policy, incluindo **PII em `text`/`tool_input`**. Lacunas menores: **teste específico do filtro de log** e **SimpleCov** |
| Segurança | 🟢 Verde | Devise + Pundit + CSRF nativo + rack-attack + CSP restrita; dump fora do versionamento; **render de turnos com auto-escape + redação de PII (F5.1.5)**. **PB-017 (entregue/publicada, `477829d`):** sem cadastro público; reset 30min + paranoid; senha ≥10; rate limit login por IP + conta/e-mail; mailer por ENV (SMTP separado); cookies SameSite=Lax/HttpOnly/Secure; checklist secure-auth aplicado (**risco residual aceito**: bcrypt custo 10 < alvo 100–300ms — não é PASS pleno; elevar = melhoria futura). **PB-018 (validado, aceite pendente):** cor de status configurável validada por allowlist hex + escape no render; integridade de status por FK no banco |
| Documentação | 🟢 Verde | Governança oficial em `app/docs`; **`PRODUCT_BACKLOG` = fonte de status/autorização** (PB-NNN); **gate de integridade formalizado**; `DELIVERY_LOG` **append-only**; **métricas correntes centralizadas no `PROJECT_STATUS`** |
| Deploy/operação | 🔴 Vermelho (prod não exercida) | Dev reproduzível (`.devstack/up.sh`); **produção NUNCA exercida**. F7.1 endureceu `production.rb` (TLS/hosts/mailer por ENV) + admin seed. Restam: Kamal/`config/deploy.yml`, schemas Solid cache/cable, worker de jobs, `/normalized`-em-prod, runbook backup/restore/rollback. Ver "Readiness de produção (F7)" abaixo |

## Checklist executivo
- [x] Fase 0 aprovada.
- [x] ADRs aprovados.
- [x] Modelo de dados aprovado.
- [x] DDL planejado revisado.
- [x] Corpus de teste definido e **criado** (`test/fixtures/normalized_corpus/`; usado nos testes).
- [x] Estratégia de sync aprovada.
- [x] Rails Foundation iniciada. **(M1 concluído — 2026-06-16)**
- [x] Domínio migrado. *(M2 concluído: CRUD completo; migração de dados reais **N/A** — RepoA inativo, domínio vazio na origem.)*
- [x] Conversas importadas. *(Metadados, F3: 1635 conversas; turnos/UI fora.)*
- [x] Vínculos implementados. *(M4 MVP manual concluído: `conversation_links` reversível/auditável + counters; scorer/auto-link/sugestões/aceite-lote/`time_entry_id` = v1/roadmap.)*
- [ ] UI unificada validada. *(**F5 MVP interno CONCLUÍDO** — render+PII+markdown+lista acionável+criar/vincular tarefa+âncoras; PB-003a timer. Pendentes p/ UI "completa": UI-01/04/09, busca/triagem, PB-013 navegação = roadmap/v1.)*
- [x] Testes mínimos concluídos. *(Suíte atual verde; cobertura **evolutiva**; teste específico do filtro de log e SimpleCov são melhorias, **não** bloqueiam o mínimo atual.)*
- [ ] Documentação finalizada.

## Readiness de produção (F7) — diagnóstico pós-F5.1.4
> **App sólido; produção NUNCA exercida.** Apto a **MVP interno single-tenant apenas após a fundação F7**. **Exposição externa/multi-tenant NÃO recomendada** sem: F7 completa + isolamento por owner/tenant (hoje ADR-014 = domínio compartilhado) + redação de PII em `text`/`tool_input`.

**Resolvido na F7.1 (2026-06-19):** `production.rb` endurecido por ENV (`force_ssl`/`assume_ssl`/`config.hosts`/mailer host) com `/up` fora do redirect; **admin seed** opt-in/idempotente (`db/seeds.rb`) seguro; **redação de PII em `text`/`tool_input`** (entregue antes na F5.1.5).

**Bloqueadores (F7) restantes antes de subir produção:**
- Schemas **Solid cache/queue/cable** ausentes (só `db/queue_schema.rb`) → `db:prepare` incompleto; `cache_store` não aponta `:solid_cache_store`.
- `cable.yml` de produção ainda em **Redis** (não `solid_cable`).
- **Kamal/deploy ausente** (`config/deploy.yml`/`.kamal/`); sem TLS/reverse-proxy/restart declarados.
- **Worker de jobs** (Solid Queue) não definido (`SOLID_QUEUE_IN_PUMA` ou `bin/jobs`).
- **`/normalized` em produção indefinido** (sem origem/volume `:ro` → conversas `:stale`).
- **Pipeline Python** sem topologia de produção (onde roda / como entrega `sessions.jsonl` / cadência de reindex `sync:turn_refs`).
- **Backup/restore/rollback** de produção pendentes (runbook).

**Não bloqueante (corrigir na F7):** entrada órfã `001 NO FILE` em `schema_migrations`; `time_zone`/`i18n.default_locale` nos defaults (UTC/:en) apesar da UI pt-BR.

**OK (já maduro):** `master.key` gitignored/nunca commitado + `credentials.yml.enc`; `filter_parameter_logging` robusto; CSP restrita; rack-attack; healthcheck `/up` + páginas de erro; Dockerfile multi-stage não-root; XSS/Pundit/CSRF cobertos por teste.

## Fronteiras do projeto
- **`_origem/` e `_mockup/` são SOMENTE LEITURA** (referência); o produto é construído do zero em `app/`.
- `_origem/_repoa` e `_origem/_repob` = referência de domínio/pipeline. `_mockup` = referência visual/de fluxo.
- Proibido copiar código/componentes/assets/arquivos das referências sem aprovação explícita (gatilho de parada).
- **Topologia (consolidada — ADR-019):** `app/` é o **repositório Git único** (produto + governança em `app/docs/`; toolchain em `app/.devstack/`). A raiz `c:\Sandbox\_omni` é apenas pasta local de trabalho/histórico (seu `.git` antigo é mantido como arquivo, sem novos commits). `_origem/` e `_mockup/` seguem fora do repo.
- Regra completa: ver [CONSTRAINTS.md](CONSTRAINTS.md).

## Governança da documentação
- **Ordem de atualização:** ARCHITECTURE_DECISIONS_INDEX → ROADMAP → MIGRATION_PLAN → FEATURE_MATRIX → DELIVERY_LOG → PROJECT_STATUS (sempre por último).
- **A cada entrega:** DELIVERY_LOG + FEATURE_MATRIX + PROJECT_STATUS (e ROADMAP se fechou marco).
- **A cada decisão arquitetural:** novo `docs/adr/ADR-NNN-*.md` + ARCHITECTURE_DECISIONS_INDEX + PROJECT_STATUS.
- **A cada mudança de escopo:** FEATURE_MATRIX + ROADMAP + PROJECT_STATUS (e MIGRATION_PLAN se afeta dados/estratégia).
